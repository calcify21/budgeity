// Preload script allows you to expose specific native APIs to your React app securely.
// For now, we keep it empty.
window.addEventListener('DOMContentLoaded', () => {
  console.log('Budgeity Desktop Loaded');
});
